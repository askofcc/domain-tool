{
  "name": "domain-tool",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "build": "node build.js",
    "dev": "node proxy.js",
    "deploy": "npx wrangler deploy",
    "deploy:temporary": "npx wrangler deploy --temporary"
  },
  "devDependencies": {
    "wrangler": "^4.22.0"
  }
}
